    /**
    *
    *     | 
    *                                
    *   engine.mouseButtons        | Returns mouse buttons states: [0] - left button, [1] - middle button, [2] - right button
    *                                - Example: engine.mouseButtons[0] (returns true if pressed and vice versa);
    *   engine.keys                | Returns keyboard states
    *                                - Example: engine.keys[fromCharCode(&#039;a&#039;)] (returns true if pressed and vice versa)
    */
fps(20);

function update()
{
    draw();
}

function draw()
{
    context.clear();
    //Here you go
}